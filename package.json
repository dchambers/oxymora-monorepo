{
  "name": "@dchambers/oxymora-monorepo",
  "packageManager": "yarn@3.3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "yarn workspaces-to-typescript-project-references",
    "compile": "yarn workspaces foreach --topological run compile"
  },
  "devDependencies": {
    "@monorepo-utils/workspaces-to-typescript-project-references": "^2.9.0"
  }
}
